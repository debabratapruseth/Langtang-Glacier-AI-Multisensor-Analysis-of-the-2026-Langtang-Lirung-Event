# Data directories

`raw/` and `processed/` are local caches. Pixel data remain in Earth Engine; small
derived tables may be exported to `processed/`. Generated research artifacts belong
under `outputs/`. Large or credential-bearing files must not be committed.

